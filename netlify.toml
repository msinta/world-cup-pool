[build]
  command = "npm run build"
  publish = "dist"
  ignore = "exit 0"  # GitHub Actions handles all builds — skip Netlify auto-builds

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
